# {{ $vault := (get (printf "%s" (env "VAULT_ADDR")) | decode_json).data }}

commands:
{{ template_file "templates/user/command1.yaml" (decode_yaml `
exec: this
path:
  - something: that
`) }}
app:
  arg1: a
  debug: true
  lists:
    - this
    - that
  listEl: {{ if and .app.lists (gt (len .app.lists) 0) }}{{ index .app.lists 1 }}{{ end }}
  vault: {{ $vault | encode_json }}
  postgreSQLUsername: {{ $vault.postgresql_username }}
